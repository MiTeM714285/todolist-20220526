<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.todolist.domain.user.UserRepository">
	<insert id="save" parameterType="com.springboot.todolist.domain.user.User" useGeneratedKeys="true" keyProperty="usercode">
  		insert into user_mst values(0, #{username}, #{oauth2_username}, #{password}, #{email}, #{name}, #{provider}, now(), now())
  	</insert>
  	
  	<select id="findUserByUsername" parameterType="String" resultType="com.springboot.todolist.domain.user.User">
  		select usercode, email, name, username, oauth2_username, password, provider, create_date, update_date
  		from user_mst
  		where username = #{username}
  		
  	</select>
  	
  	<select id="findOAuth2UserByOAuth2Username" parameterType="String" resultType="com.springboot.todolist.domain.user.User">
  		select usercode, email, name, username, oauth2_username, password, provider, create_date, update_date 
  		from user_mst 
  		where oauth2_username = #{oauth2_username}
  	</select>
</mapper>